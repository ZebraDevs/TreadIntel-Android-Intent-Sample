<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/background" />  <!-- You can change the color here -->
    <corners
        android:topRightRadius="25dp"
        android:topLeftRadius="25dp"/> <!-- Here is the 5dp radius -->
    <stroke
        android:width="1dp"
        android:color="@color/disable" />
</shape>
